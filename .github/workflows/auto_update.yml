name: Daily Box Office Update

on:
  schedule:
    - cron: '0 * * * *'  # 設定每小時執行一次
  workflow_dispatch:      # 保留手動測試按鈕

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write    # 重要：這行賦予機器人修改檔案並上傳的權限
    steps:
      - uses: actions/checkout@v3
      
      - name: 設定 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: 執行更新腳本
        run: python update_box_office.py
        
      - name: 將變更推回 GitHub
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/index.json
          # 如果沒有變更就不會報錯
          git commit -m "Auto-update: $(date)" || echo "無資料變動"
          git push